<div>

    {{#each users as |user|}}
        {{!-- name of partial --}}
        {{> test-melon-user}}
        {{else}}
        <div><p>there are currently no users, check back later!</p></div>
    {{/each}}

</div>